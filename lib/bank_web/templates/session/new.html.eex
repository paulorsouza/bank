<h1>Login</h1>

<%= form_for @conn, Routes.session_path(@conn, :create), [as: :session], fn f -> %>
  <%= label f, "Username or email" %>
  <%= text_input f, :credential %>
  
  <%= label f, :password %>
  <%= password_input f, :password %>
  
  <%= submit "Log in" %>
<% end %>
